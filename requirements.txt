Flask==2.3.3
PyJWT==2.8.0
Werkzeug==2.3.7
python-dotenv==1.0.0
pytest==9.1.1

# Optional — only needed if you later add an ANTHROPIC_API_KEY to enable
# live AI responses. The app runs fully without it (uses the built-in
# local guide assistant instead). Install separately when you're ready:
#   pip install requests==2.32.3
# requests==2.32.3